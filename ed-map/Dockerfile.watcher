FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    psycopg2-binary==2.9.10

# etl.py provides the shared parsing/insert logic; watcher.py imports it
COPY etl.py schema.sql watcher.py ./

CMD ["python", "/app/watcher.py"]
